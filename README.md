Martian Robots
==============

Prerequisites:
[Node.js](http://nodejs.org/) and [npm](http://npmjs.org/)


You can run code with:

	npm install
    npm run dev

You can run the tests with:

    npm run test

